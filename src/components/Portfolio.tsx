import Section from '@/components/Section';

const Portfolio = () => {
  return (
    <Section title='Portfolio' className='h-[600px] bg-red-500'>
      <h2 className='text-white text-lg font-bold'>Portfolio</h2>
    </Section>
  );
};

export default Portfolio;
