import Section from '@/components/Section';

const Hero = () => {
  return (
    <Section title='Home' className='md:pt-20 h-[600px] bg-red-500'>
      <h2 className='text-white text-lg font-bold'>Home</h2>
    </Section>
  );
};

export default Hero;
